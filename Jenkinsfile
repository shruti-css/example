pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                echo 'Code pulled from GitHub successfully'
            }
        }

        stage('Build Step') {
            steps {
                echo 'Build running in Jenkins'
            }
        }
    }
}
